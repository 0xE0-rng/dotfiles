# File: ~/.config/direnv/direnvrc
if type direvn &> /dev/null; then
  source "$(asdf direnv hook asdf)"
fi

# Uncomment the following line to make direnv silent by default.
# export DIRENV_LOG_FORMAT=""
